# Which implementation /api/fill-form uses. "mock" | "openai"
# Read at runtime via $env/dynamic/private, so changing it in the Vercel
# dashboard takes effect on redeploy without a code change.
FILL_FORM_PROVIDER=mock

# Only required once FILL_FORM_PROVIDER=openai. Leave empty until then.
OPENAI_API_KEY=
