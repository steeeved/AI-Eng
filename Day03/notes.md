# Tokenization

Tokenization - process of translating text into numbers (tokens) and vice-versa.
LLMs are worse at non-english language tokenization.


Have we gone the stage where LLMs are unable to spell words?
Can LLMs do super simple string processing tasks like reversing a string?
Are LLMs better at non-english languages now - (Like how better)?
Are LLMs better at simple arithmetics?
Why should I prefer to use YAML over JSON with LLMs?

